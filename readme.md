# Server Hopper

[Velocity](https://papermc.io/software/velocity/) plugin that provides a dialog to help players hop between servers.

## Commands

- `servers`: Open the server switcher dialog.

### Permissions

- `serverhopper.command.servers`: Allows usage of `/servers`.
